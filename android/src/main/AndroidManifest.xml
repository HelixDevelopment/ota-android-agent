<?xml version="1.0" encoding="utf-8"?>
<!--
  Helix OTA — Android agent library manifest (system-UID path; integration guide §10).
  System-UID + platform signing is what unlocks the @SystemApi UpdateEngine. Alternative:
  drop sharedUserId and ship as a privileged app under /system/priv-app (still platform-signed).
  The agent is shipped as part of the system image, NOT a Play-Store app.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!--
      No third-party permission grants UpdateEngine access; it is gated by
      @SystemApi + system-UID/priv-app + SELinux + /data/ota_package access.
    -->
</manifest>
